import useMatch from './useMatch';

/** Returns the current route params */
export default function useParams() {
  var _useMatch;
  return (_useMatch = useMatch()) == null ? void 0 : _useMatch.params;
}