"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");
exports.__esModule = true;
exports.default = getStoreRenderArgs;
var _createStoreRouterObject = _interopRequireDefault(require("./createStoreRouterObject"));
var _getRenderArgs = _interopRequireDefault(require("./getRenderArgs"));
// This function returns a promise. It doesn't need to be an async function
// because it doesn't use the promise's value.
function getStoreRenderArgs({
  store,
  // TODO: check types of this
  getFound = ({
    found
  }) => found,
  matchContext,
  resolver
}) {
  const router = (0, _createStoreRouterObject.default)(store);
  const match = getFound(store.getState()).resolvedMatch;
  return (0, _getRenderArgs.default)(router, {
    match,
    matchContext,
    resolver
  });
}
module.exports = exports.default;