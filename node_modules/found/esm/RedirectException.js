// This isn't really an error.
export default class RedirectException {
  constructor(location, status = 302) {
    this.isFoundRedirectException = true;
    this.location = location;
    this.status = status;
  }
}